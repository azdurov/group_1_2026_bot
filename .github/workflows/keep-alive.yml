name: Keep Render Alive

on:
  schedule:
    # каждые 10 минут с 06:00 до 23:59 UTC
    - cron: "*/10 3-20 * * *"

  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Render
        run: |
          curl -fsS "${{ vars.RENDER_URL }}" || true